{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "buildplugin",
            "type": "process",
            "command": "py",
            "args": [
                "../../pluginpack.py",
                "--with-version",
                "--mask",
                "(?:cdol-custom-pscb-report-navigation\\.json|cdol-custom-pscb-student-navigation\\.json|README\\.md)$"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
